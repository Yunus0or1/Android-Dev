<?xml version="1.0" encoding="utf-8"?>

<!-- These two items make one item and then this is inflated as bottom nav
 navigation Icon. This part is very important in designing as it shows
 how to add two or more items in one drawable image. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape
            android:innerRadius="25dp"
            android:shape="ring"
            android:thickness="5dp"
            android:useLevel="false">
            <solid android:color="@color/off_red" />
            <padding
                android:left="11dp"
                android:right="11dp"/>
        </shape>
    </item>

    <item
        android:drawable="@drawable/ic_home"
        android:width="40dp"
        android:height="50dp" />

</layer-list>